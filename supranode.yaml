script:
  ## Install dependencies (alternatives commented)
  # - yarn install
  # - pnpm install
  # - bun install
  - npm install
  ## Build (alternatives commented)
  # - solid-start build
  - npm run build
artifact: .output
