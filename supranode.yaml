script:
  ## Install dependencies (alternatives commented)
  # - yarn install
  # - pnpm install
  # - bun install
  - npm install
  ## Build (alternatives commented)
  # - next export
  - npm run build
artifact: out
